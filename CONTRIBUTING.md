# 기여 가이드 (CONTRIBUTING GUIDE)
## 1. 일반 개발 규칙

- **브랜치 전략**: `feature/{issue-number}-{short-description}` 형식으로 브랜치를 생성합니다. (예: `feature/123-add-product-api`)
- **커밋 메시지**: [Conventional Commits](https://www.conventionalcommits.org/ko/v1.0.0/) 명세를 따릅니다.
  - `feat`: 새로운 기능 추가
  - `fix`: 버그 수정
  - `docs`: 문서 수정
  - `style`: 코드 포맷팅, 세미콜론 누락 등 (코드 변경 없음)
  - `refactor`: 코드 리팩토링
  - `test`: 테스트 코드 추가/수정
  - `chore`: 빌드 관련 파일 수정, 패키지 매니저 설정 등
- **Pull Request (PR)**:
  - PR 템플릿(`.github/PULL_REQUEST_TEMPLATE.md`)을 준수하여 작성합니다.
  - 최소 1명 이상의 리뷰어에게 승인을 받아야 합니다.
  - CI 빌드가 성공해야 합니다.
- **코드 스타일**: `.editorconfig`와 `CODE_CONVENTIONS.md` 가이드를 따릅니다.

## 2. AI 협업 규칙 (프롬프트 가이드)

AI(GitHub Copilot)와 함께 작업할 때 다음 규칙을 따라주세요.

1.  **명확한 목표 제시**: "X를 해주세요" 와 같이 명확하고 구체적인 목표를 제시해주세요.
    - (좋은 예): "ProductRequestDto에 수량(quantity) 필드를 추가하고, 유효성 검증 로직을 넣어줘."
    - (나쁜 예): "상품 관련해서 수정해줘."
2.  **충분한 컨텍스트 제공**: AI가 작업을 이해하는 데 필요한 파일이나 코드를 함께 제공해주세요.
    - 에디터에서 관련된 파일을 열어두거나, 특정 코드를 복사해서 질문에 포함하면 좋습니다.
3.  **작업 단위 분리**: 크고 복잡한 작업은 한 번에 요청하기보다 여러 단계로 나누어 요청해주세요.
    - (예): 1) "엔티티 클래스 먼저 만들자." -> 2) "이제 이 엔티티를 사용하는 Repository를 만들어줘."
4.  **피드백 제공**: AI가 생성한 코드가 의도와 다를 경우, 어떤 부분이 어떻게 잘못되었는지 구체적으로 알려주세요.
    - (예): "할인율 계산 로직이 틀렸어. 퍼센트가 아니라 고정 금액을 빼야 해."
5.  **스타일 및 컨벤션 명시**: 특정 스타일이나 아키텍처 패턴을 따라야 할 경우, 명확하게 언급해주세요.
    - (예): "JPA Auditing을 사용해서 생성/수정 시간을 자동으로 기록하도록 만들어줘."

이 규칙들은 원활한 소통과 높은 품질의 결과물을 얻기 위해 만들어졌습니다.
